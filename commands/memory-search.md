---
description: Search across all memory files for matching content
agent: build
---

Search for "$ARGUMENTS" across all memory files using the memory_search tool. Show the results with file paths and line numbers.
